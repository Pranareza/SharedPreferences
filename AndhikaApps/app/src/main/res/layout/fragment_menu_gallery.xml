<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_content_tab"
    tools:context=".Menu.ui.gallery.GalleryFragment">

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="@string/txtview_wallpaper"
        android:gravity="center"
        android:textSize="@dimen/font31"
        android:textColor="@android:color/white"
        android:layout_marginBottom="@dimen/dimen10"
        android:layout_marginTop="@dimen/dimen5"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_gallery"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical"
        android:layout_below="@id/textView"
        android:layout_marginBottom="@dimen/dimen3"/>
</RelativeLayout>